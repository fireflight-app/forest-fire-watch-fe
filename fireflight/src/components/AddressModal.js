import React from 'react';
import Modal from 'react-modal';

const AddressModal = (props) => (
  <Modal
  isOpen={true}
  contentLabel='address input'
  >
  <h3>selected option</h3>
  </Modal>
)

export default AddressModal;